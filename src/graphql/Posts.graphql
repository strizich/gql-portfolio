query($first: Int!, $skip: Int!){ 
  posts(first: $first, skip: $skip){
    id
    title
    postType
    slug
    updatedAt
    createdAt
    featured
    status
    featuredImage{
      handle
      width
      height
      id
    }
  }
}
