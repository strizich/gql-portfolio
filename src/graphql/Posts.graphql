query($first: Int!, $skip: Int!){ 
  posts(first: $first, skip: $skip){
    id
    title
    postType
    slug
    updatedAt
    featured
    featuredImage{
      handle
      width
      height
      id
    }
  }
}
