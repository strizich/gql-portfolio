query($first: Int!, $skip: Int!){ 
  posts(first: $first, skip: $skip){
    id
    title
    postType
    slug
    updatedAt
    createdAt
    featured
    status
    content
    featuredImage{
      handle
      width
      height
      id
    }
  }
}
