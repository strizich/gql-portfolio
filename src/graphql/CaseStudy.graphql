#import './CaseStudyDetails.graphql'
query($slug: String!){ 
  caseStudy(where: {slug: $slug}) {
    id
    updatedAt
    createdAt
    title
    postType
    slug
    content
    featuredImage{
      url
      id
    }
    projectDetail{
      ...details
    }
  }
}
