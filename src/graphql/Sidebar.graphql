query($slug: String!){
  about(where:{slug:$slug}){
    firstName
    lastName
    title
    email
    phone
  	userSkills{
      id
      name
      rating
      description
      featured
    }
  }
}