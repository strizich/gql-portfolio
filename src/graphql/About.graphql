query($slug: String!){
  about(where:{slug:$slug}){
    firstName
    lastName
    title
    email
    phone
    introduction
    content
    userSkills {
      id
      name
      rating
      description
      featured
    }
  }
}
